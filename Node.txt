npm init -y
npm i express
node --watch index.js
import express from 'express'
const app=express();
middle ware:
    app.use(express.json());(use to convert body into Json and wise versa)

res.status(400).json({
        status: true,
        message: "All the Suits Available",
        data: []
})
MVC(Model View Controller):
    controller(work of specific route):
    Models(Schema and model):
    Routes(from index direct to specific domain and define auth routes,products routes):
    ENV : private data aur links
    gitignore: not send on git